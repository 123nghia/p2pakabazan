# Shared development/staging profile
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/p2p_trading_dev}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres123}
spring.datasource.hikari.maximum-pool-size=${DB_POOL_SIZE:15}

spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}

jwt.secret=HJG67sd87sdhjJJH873hjhHGGFds87ds89ds78HJH7ds7fsdfgd==

server.port=${SERVER_PORT:9999}

# DataSeeder configuration
app.seed.enabled=${APP_SEED_ENABLED:false}

# Local defaults for SSO + UI testing (override via env vars in real dev/staging)
app.sso.partners=${APP_SSO_PARTNERS:SAN_A:change-me}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:8085,http://localhost:9898,http://192.168.1.17:8085,http://192.168.1.17:9898,http://192.168.1.17:5174,http://192.168.1.89:5174, http://localhost:5174}
app.sso.admin-token=${APP_SSO_ADMIN_TOKEN:change-me}

# Partner internal API (P2P -> Partner) for local testing
app.partner-api.partners=${APP_PARTNER_API_PARTNERS:SAN_A=https://luggage-catalyst-surf-kits.trycloudflare.com}
app.partner-api.client-id=${APP_PARTNER_API_CLIENT_ID:P2P_APP}
app.partner-api.client-secret=${APP_PARTNER_API_CLIENT_SECRET:change-me}
